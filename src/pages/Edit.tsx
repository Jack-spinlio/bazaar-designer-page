
import React from 'react';
import { Layout } from '@/components/Layout';

const Edit = () => {
  return (
    <Layout>
      <div className="p-6">
        {/* Text removed as requested */}
      </div>
    </Layout>
  );
};

export default Edit;
