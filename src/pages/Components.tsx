
import React from 'react';
import { Layout } from '@/components/Layout';

const Components = () => {
  return (
    <Layout>
      {/* Content area is intentionally empty since we're using the sidebar for displaying components */}
    </Layout>
  );
};

export default Components;
